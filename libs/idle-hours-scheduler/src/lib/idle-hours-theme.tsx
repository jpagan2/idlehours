import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#FD629B',
      contrastText: '#FFFFFF',
    },
    secondary: {
      main: '#FEA36B',
    },

    background: { default: '#323337' },
  },
});
